<article class="ac-page-shell">
  <header class="ac-section-heading ac-section-heading-page">
    <div>
      <p class="ac-eyebrow">Static page</p>
      <h1><%= page.title || '页面' %></h1>
    </div>
  </header>

  <% if (page.content && page.content.trim()) { %>
    <div class="ac-prose">
      <%- page.content %>
    </div>
  <% } else { %>
    <section class="ac-placeholder-card">
      <h2>这里还在布置中</h2>
      <p>这个页面已经接入新的 Hexo 主题视觉系统，后续可以继续补充更多固定内容。</p>
    </section>
  <% } %>

  <% if (String(page.title || '').toLowerCase() === 'about') { %>
    <section class="ac-info-grid">
      <article class="ac-info-card">
        <span>发布链路</span>
        <strong>Obsidian → GitHub → Pages</strong>
        <p>保留现有同步脚本和 Hexo 构建流程，只替换主题表现层。</p>
      </article>
      <article class="ac-info-card">
        <span>站点内容</span>
        <strong><%= site.posts ? site.posts.length : 0 %> 篇文章 / <%= site.tags ? site.tags.length : 0 %> 个标签</strong>
        <p>教程、工具记录、独立开发和自动化实践都会持续沉淀在这里。</p>
      </article>
      <article class="ac-info-card">
        <span>当前主题</span>
        <strong>ac-site-template Hexo 版</strong>
        <p>以参考模板的配色、排版和图形语言为基础，兼容现有流程。</p>
      </article>
    </section>
  <% } %>
</article>
